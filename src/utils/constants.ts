export enum ROUTE {
  INDEX = '/',
  POSTS = '/posts',
  COMMENTS = '/comments',
}

export const API_URL = 'https://jsonplaceholder.typicode.com';

export enum API_ROUTES {
  POSTS = '/posts',
  COMMENTS = '/comments',
}
